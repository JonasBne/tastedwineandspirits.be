---
const { heading, size } = Astro.props as { heading: string, size: 'small' | 'normal' };

const textSize = size === 'small' ? 'text-lg' : 'text-2xl';
---

<h4 class={`text-rose-primary font-bold ${textSize}`}>
    { heading }
</h4>